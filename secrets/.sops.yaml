# sops 配置文件：管理本仓库中的加密机密。
# 部署前需为每台主机生成 age 密钥对，并将公钥加入 .sops.yaml。
# 详情参见 docs/DETAILS.md 的 Secrets 章节。

creation_rules:
  # 默认规则：使用主机公钥加密
  - path_regex: secrets/.+\.yaml$
    age: |
      # 示例（请替换为各主机实际 age 公钥）：
      # age1...  # desktop
      # age1...  # laptop
      # age1...  # server

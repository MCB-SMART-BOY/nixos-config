// Niri Configuration
// ~/.config/niri/config.kdl
// A beautiful scrollable tiling Wayland compositor

// ══════════════════════════════════════════════════════════════════
// 🖥️ Output Configuration
// ══════════════════════════════════════════════════════════════════

// 建议删除特定 output 配置，让 Niri 自动检测
// 如果必须指定，请确保名称 (eDP-1) 是准确的
output "eDP-1" {
    mode "2560x1600@60"
    scale 2 // 推荐高分屏设置缩放
}

// ══════════════════════════════════════════════════════════════════
// ⌨️ Input Configuration
// ══════════════════════════════════════════════════════════════════

input {
    keyboard {
        xkb {
            layout "us"
            // options "caps:escape" // 如果你是 Vim 用户，建议开启
        }
        repeat-delay 300
        repeat-rate 50
    }

    touchpad {
        tap
        dwt
        natural-scroll
        accel-speed 0.2
        accel-profile "adaptive"
    }

    mouse {
        accel-speed 0.0
        accel-profile "flat"
    }

    focus-follows-mouse max-scroll-amount="0%"
}

// ══════════════════════════════════════════════════════════════════
// 🎨 Layout Configuration
// ══════════════════════════════════════════════════════════════════

layout {
    gaps 14
    center-focused-column "always"

    preset-column-widths {
        proportion 0.33333
        proportion 0.5
        proportion 0.66667
    }

    default-column-width { proportion 0.97; }

    focus-ring {
        width 2
        active-color "#7dd3fc"     // Accent
        inactive-color "#253248"   // Border
    }

    border {
        off
    }

    shadow {
        on
        softness 30
        spread 5
        offset x=0 y=5
        color "#00000070"
    }
}

// ══════════════════════════════════════════════════════════════════
// 🖼️ Window Decorations
// ══════════════════════════════════════════════════════════════════

prefer-no-csd

// ══════════════════════════════════════════════════════════════════
// ✨ Animations (优化版)
// ══════════════════════════════════════════════════════════════════

animations {
    slowdown 1.0

    workspace-switch {
        spring damping-ratio=1.0 stiffness=1000 epsilon=0.0001
    }

    window-open {
        duration-ms 200
        curve "ease-out-expo"
    }

    window-close {
        duration-ms 150
        curve "ease-out-quad"
    }

    horizontal-view-movement {
        spring damping-ratio=1.0 stiffness=800 epsilon=0.0001
    }

    window-movement {
        spring damping-ratio=1.0 stiffness=800 epsilon=0.0001
    }

    window-resize {
        spring damping-ratio=1.0 stiffness=800 epsilon=0.0001
    }

    config-notification-open-close {
        spring damping-ratio=0.6 stiffness=1000 epsilon=0.001
    }
}

// ══════════════════════════════════════════════════════════════════
// 📸 Screenshot (Niri 原生)
// ══════════════════════════════════════════════════════════════════
// 注意：更高级的截图在下面的 Key Bindings 里配置了 grim+slurp

screenshot-path "~/Pictures/Screenshots/Screenshot_%Y-%m-%d_%H-%M-%S.png"

// ══════════════════════════════════════════════════════════════════
// 🚀 Autostart (自启动)
// ══════════════════════════════════════════════════════════════════

spawn-at-startup "niri-run" "waybar"
// 随机壁纸脚本改由 systemd 用户服务启动（graphical-session.target），更稳定
// NetworkManager 托盘（可在 Waybar 托盘显示）
spawn-at-startup "niri-run" "nm-applet"
spawn-at-startup "niri-run" "mako"
spawn-at-startup "niri-run" "fcitx5" "-d" "-r" // -r 替换现有实例
spawn-at-startup "niri-run" "swayidle" "-w"

// ══════════════════════════════════════════════════════════════════
// 🪟 Window Rules
// ══════════════════════════════════════════════════════════════════

window-rule {
    geometry-corner-radius 16
    clip-to-geometry true
}

window-rule {
    match app-id=r#"^(pavucontrol|nm-connection-editor|blueman-manager|org.keepassxc.KeePassXC)$"#
    open-floating true
}

window-rule {
    match app-id="firefox" title="Picture-in-Picture"
    open-floating true
}

// ══════════════════════════════════════════════════════════════════
// ⌨️ Key Bindings
// ══════════════════════════════════════════════════════════════════

binds {
    // ─── 应用启动 ───
    Mod+Return { spawn "niri-run" "sh" "-c" "${TERMINAL:-foot}"; }
    Mod+Space { spawn "niri-run" "fuzzel"; }
    // Mod { spawn "fuzzel"; }
    Mod+E { spawn "niri-run" "nautilus"; }
    Mod+B { spawn "niri-run" "firefox"; }

    // ─── 系统控制 ───
    Mod+Q { close-window; }
    Mod+Shift+Q { quit; } // 退出 Niri 会话
    Mod+Shift+Slash { show-hotkey-overlay; }
    
    // 锁屏
    Mod+Escape { spawn "niri-run" "sh" "-c" "[ -x \"$HOME/.local/bin/lock-screen\" ] && \"$HOME/.local/bin/lock-screen\" || swaylock"; }
    
    // 电源管理
    Mod+Shift+P { power-off-monitors; }
    // 切换壁纸
    Mod+Shift+C { spawn "niri-run" "wallpaper-random"; }

    // ─── 焦点移动 (Vim 风格) ───
    Mod+H { focus-column-left; }
    Mod+J { focus-window-down; }
    Mod+K { focus-window-up; }
    Mod+L { focus-column-right; }
    
    Mod+Left { focus-column-left; }
    Mod+Down { focus-window-down; }
    Mod+Up { focus-window-up; }
    Mod+Right { focus-column-right; }

    // ─── 窗口移动 ───
    Mod+Shift+H { move-column-left; }
    Mod+Shift+J { move-window-down; }
    Mod+Shift+K { move-window-up; }
    Mod+Shift+L { move-column-right; }
    
    Mod+Shift+Left { move-column-left; }
    Mod+Shift+Down { move-window-down; }
    Mod+Shift+Up { move-window-up; }
    Mod+Shift+Right { move-column-right; }

    // ─── 快速跳转 ───
    Mod+Home { focus-column-first; }
    Mod+End { focus-column-last; }
    Mod+Shift+Home { move-column-to-first; }
    Mod+Shift+End { move-column-to-last; }

    // ─── 布局操作 ───
    Mod+BracketLeft { consume-or-expel-window-left; }
    Mod+BracketRight { consume-or-expel-window-right; }
    Mod+R { switch-preset-column-width; }
    Mod+Shift+R { reset-window-height; }
    Mod+Shift+F { maximize-column; }
    Mod+F { fullscreen-window; }
    Mod+C { center-column; }

    // 微调大小
    Mod+Minus { set-column-width "-10%"; }
    Mod+Equal { set-column-width "+10%"; }
    Mod+Shift+Minus { set-window-height "-10%"; }
    Mod+Shift+Equal { set-window-height "+10%"; }

    // ─── 工作区 (Workspaces) ───
    Mod+1 { focus-workspace 1; }
    Mod+2 { focus-workspace 2; }
    Mod+3 { focus-workspace 3; }
    Mod+4 { focus-workspace 4; }
    Mod+5 { focus-workspace 5; }
    Mod+6 { focus-workspace 6; }
    Mod+7 { focus-workspace 7; }
    Mod+8 { focus-workspace 8; }
    Mod+9 { focus-workspace 9; }
    
    Mod+Shift+1 { move-column-to-workspace 1; }
    Mod+Shift+2 { move-column-to-workspace 2; }
    Mod+Shift+3 { move-column-to-workspace 3; }
    Mod+Shift+4 { move-column-to-workspace 4; }
    Mod+Shift+5 { move-column-to-workspace 5; }
    Mod+Shift+6 { move-column-to-workspace 6; }
    Mod+Shift+7 { move-column-to-workspace 7; }
    Mod+Shift+8 { move-column-to-workspace 8; }
    Mod+Shift+9 { move-column-to-workspace 9; }

    Mod+Tab { focus-workspace-down; }
    Mod+Shift+Tab { focus-workspace-up; }

    // ─── 截图 (高级模式) ───
    // 依赖: grim, slurp, swappy (需在 systemPackages 中安装)
    // 截图全屏
    Print { spawn "niri-run" "sh" "-c" "grim - | swappy -f -"; }
    // 截图选区 (推荐)
    Shift+Print { spawn "niri-run" "sh" "-c" "grim -g \"$(slurp)\" - | swappy -f -"; }
    // 原生截图 (备用)
    Alt+Print { screenshot; }

    // ─── 媒体与亮度 ───
    XF86AudioRaiseVolume allow-when-locked=true { spawn "niri-run" "wpctl" "set-volume" "@DEFAULT_AUDIO_SINK@" "5%+"; }
    XF86AudioLowerVolume allow-when-locked=true { spawn "niri-run" "wpctl" "set-volume" "@DEFAULT_AUDIO_SINK@" "5%-"; }
    XF86AudioMute allow-when-locked=true { spawn "niri-run" "wpctl" "set-mute" "@DEFAULT_AUDIO_SINK@" "toggle"; }
    XF86AudioMicMute allow-when-locked=true { spawn "niri-run" "wpctl" "set-mute" "@DEFAULT_AUDIO_SOURCE@" "toggle"; }
    XF86MonBrightnessUp allow-when-locked=true { spawn "niri-run" "brightnessctl" "set" "5%+"; }
    XF86MonBrightnessDown allow-when-locked=true { spawn "niri-run" "brightnessctl" "set" "5%-"; }
}

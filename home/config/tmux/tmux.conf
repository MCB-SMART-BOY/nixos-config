# Tmux Configuration
# ~/.config/tmux/tmux.conf
# Geek-friendly defaults: vi keys, fast response, big history

# Basic UX
set -g mouse on
set -g history-limit 100000
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on
set -g status-interval 5

# Vi-style selection and movement in copy mode
setw -g mode-keys vi
set -g status-keys vi

# Faster key response (reduces delay after prefix)
set -s escape-time 0

# Clipboard integration (OSC52 where supported)
set -g set-clipboard on

# True color support
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",xterm-256color:Tc"

# Simple status theme (Catppuccin-like)
set -g status-style "bg=#1e1e2e,fg=#cdd6f4"
set -g status-left "#[fg=#89b4fa,bold] #S "
set -g status-right "#[fg=#a6e3a1] %Y-%m-%d %H:%M "
set -g pane-border-style "fg=#45475a"
set -g pane-active-border-style "fg=#cba6f7"

# Split panes quickly
bind | split-window -h
bind - split-window -v

# Vim-style pane navigation
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Resize panes with Shift+H/J/K/L
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# Reload config quickly
bind r source-file ~/.config/tmux/tmux.conf \; display-message "tmux.conf reloaded"
